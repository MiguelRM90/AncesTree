node_modules/
dist/
.DS_Store
*.local
.vite/

# Generated stress-test projects. The generator is committed, its output is not.
stress-*/

# The working specification is written in Spanish and stays local. README.md is
# the one document that ships, and it is in English.
*.md
!README.md
vision.txt

# Never, under any circumstance, real family data in the repository.
# Test fixtures are anonymised and live in test/fixtures/.
*.ged
!test/fixtures/**/*.ged
family.json
manifest.json
!public/manifest.webmanifest
photos/
documents/
backups/
